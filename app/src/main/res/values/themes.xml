<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculadora" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.Calculadora" parent="Base.Theme.Calculadora" />

    <style name="CalculatorButton">
        <item name="android:layout_margin">5dp</item>
        <item name="android:textSize">50sp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:borderlessButtonStyle">?android:attr/borderlessButtonStyle</item>
    </style>

    <style name="CalculatorButton.Number" parent="CalculatorButton">
        <item name="android:backgroundTint">@color/button_number_background</item>
        <item name="android:textColor">@color/button_number_text</item>
    </style>

    <style name="CalculatorButton.Operator" parent="CalculatorButton">
        <item name="android:backgroundTint">@color/button_operator_background</item>
        <item name="android:textColor">@color/button_operator_text</item>
    </style>

    <style name="CalculatorButton.Other" parent="CalculatorButton">
        <item name="android:backgroundTint">@color/button_others_background</item>
        <item name="android:textColor">@color/button_others_text</item>
    </style>

    <style name="CalculatorButton.Special" parent="CalculatorButton">
        <item name="android:backgroundTint">@color/button_special_background</item>
        <item name="android:textColor">@color/button_special_text</item>
    </style>
</resources>